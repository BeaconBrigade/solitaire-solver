[package]
name = "solitaire-game"
version = "0.2.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[features]
default = ["standard"]
standard = []
kplus = []

[dependencies]
rand = "0.8.5"
